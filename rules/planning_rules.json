{
  "min_steps": 3,
  "max_steps": 8,
  "rules": [
    {
      "name": "clear_step",
      "description": "Each step must be clear and easy to understand"
    },
    {
      "name": "actionable_step",
      "description": "Each step must describe a concrete action"
    },
    {
      "name": "logical_order",
      "description": "Steps must follow a sensible order"
    },
    {
      "name": "no_duplicates",
      "description": "Steps must not repeat the same action"
    },
    {
      "name": "short_plan",
      "description": "Total number of steps must stay within limits"
    }
  ]
}